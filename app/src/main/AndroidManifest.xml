<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ssarori.mbti_tmi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".recordActivity"></activity>
        <activity android:name=".allTypeActivity" />
        <activity android:name=".fourTypeActivity" />
        <activity android:name=".SNActivity" />
        <activity android:name=".PJActivity" />
        <activity android:name=".TFActivity" />
        <activity android:name=".EIActivity" />
        <activity android:name=".gunghabActivity" />
        <activity
            android:name=".splashActivity"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>